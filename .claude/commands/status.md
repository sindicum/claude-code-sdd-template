# status

現在のステアリングファイルの進捗状況を表示する。

## 実行手順

1. `.steering/` ディレクトリを検索し、最新の作業ディレクトリを特定
2. `tasklist.md` を読み込み、タスクの状態を集計
3. 以下の形式でコンソールに出力

## 実行フロー

### Step 1: ステアリングディレクトリの検索

```
Glob(".steering/*/tasklist.md")
```

最新の日付のディレクトリを特定する（命名規則: `YYYYMMDD-[feature-name]`）

### Step 2: tasklist.md の解析

タスクの状態を以下のパターンで判定:

- `- [x]`: 完了
- `- [ ]` + 進行中の記述: 進行中
- `- [ ]`: 未着手

### Step 3: 結果の出力

以下の形式でコンソールに出力:

```
📁 アクティブ: .steering/YYYYMMDD-feature-name/

📊 進捗状況:
  ✅ 完了: X/Y
  🔄 進行中: X/Y
  ⏳ 未着手: X/Y

📋 次のタスク:
  - [未着手の最初のタスク名]

📄 関連ファイル:
  - requirements.md: [存在する/存在しない]
  - design.md: [存在する/存在しない]
  - tasklist.md: [存在する/存在しない]
```

## ステアリングディレクトリが存在しない場合

```
📁 アクティブなステアリングファイルがありません。

💡 新しい作業を開始するには:
  > /add-feature [機能名]
```

## 使用ツール

- Glob: ディレクトリ検索
- Read: tasklist.md の読み込み
